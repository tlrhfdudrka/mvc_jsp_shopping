/**
 * 
 */
 
 //login 버튼 클릭시 넘어가기
        function login() {
            // JOIN US 버튼 클릭 시 회원가입 페이지로 이동
            let link = "../login/login.jsp";
        	window.open(link);
        }
        
//home 버튼 클릭시 넘어가기
        function main() {
            // JOIN US 버튼 클릭 시 회원가입 페이지로 이동
            let link = "../main/index.jsp";
        	window.open(link);
        }